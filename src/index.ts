import {getFeedData, getFeedDataFor, unsubscribeFeedFor} from "./feeder";
import {addCORSHeader} from "./cors";

export {
    addCORSHeader,
    unsubscribeFeedFor,
    getFeedData,
    getFeedDataFor
}
